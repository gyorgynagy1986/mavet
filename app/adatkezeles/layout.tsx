import type { Metadata } from "next"
export const metadata: Metadata = { title: "Adatkezelési tájékoztató" }
export default function PrivacyLayout({ children }: LayoutProps<"/adatkezeles">) { return children }
